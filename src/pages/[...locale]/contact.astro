---
const { locale } = Astro.params;

import { getStaticPaths } from "../../utils/staticPaths";

import Layout from "../../layouts/Layout.astro";

export { getStaticPaths };
---
<Layout title="En contacto" description="Escríbeme, me encantaría saber sobre tu próximo proyecto" active="contact">
  Contact page {locale}
</Layout>
