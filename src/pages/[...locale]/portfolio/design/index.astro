---
import { getStaticPaths } from "../../../../utils/staticPaths";
import { routes } from "../../../../routes";
import { getLocaleFromUrl, useTranslations } from "../../../../utils/i18n/utils";

import {
  projectHelloAPI,
  projectIxkik,
} from "../../../../utils/content/projects";

import Layout from "../../../../layouts/Layout.astro";
import ProjectCard from "../../../../components/ProjectCard.astro";
import Modal from '../../../../components/Modal.astro'

const locale = getLocaleFromUrl(Astro.url);
const t = useTranslations(locale);

const title = t('portfolio.design.index.title');
const description = t('portfolio.design.index.description');

const helloAPI = projectHelloAPI({ locale });
const ixkik = projectIxkik({ locale });

console.log(helloAPI);
export { getStaticPaths };
---
<script src="/src/scripts/projects.js"></script>

<Layout title= {title} description={description} active="portfolio"> 
  <section id="design" class="section projects">
    <ol class="breadcrumb">
      <li>
        <a href={routes.root({ locale })} class="text-subtitle">
          Inicio
        </a>
      </li>
      <li>
        <a href={routes.portfolio.root({ locale })} class="text-subtitle">
          Portafolio
        </a>
      </li>
      <li>
        <span>Diseño</span>
      </li>
    </ol>

    <div class="title">
      <p class="text-title text-uppercase">{t('portfolio.design.title')}</p>
      <h4 class="text-large">{t('portfolio.design.subtitle')}</h4>
    </div>

    <div class="content">
      <ul class="projects-cards">
        <li>
          <ProjectCard
            imageSrc={helloAPI.imageSrc}
            name={helloAPI.name}
            summary={helloAPI.summary}
            year={helloAPI.year}
            client={helloAPI.client}
            projectUrl={helloAPI.projectUrl}
            videoUrl={helloAPI.videoUrl} />
        </li>
        <li>
          <ProjectCard
            imageSrc={ixkik.imageSrc}
            name={ixkik.name}
            summary={ixkik.summary}
            year={ixkik.year}
            client={ixkik.client}
            projectUrl={ixkik.projectUrl}
            videoUrl={ixkik.videoUrl} />
        </li>
      </ul>
    </div>
  </section>

  <Modal />
</Layout>
